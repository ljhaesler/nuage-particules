const config = {
  density: 0.01, // nombre de particules par pixel
  xSpeed: 1, // speed at which the particles move
  ySpeed: 1,
  trailLength: 64, // number of frames the trail will exist for
  pAlpha: 1,
  maxColorSat: 255,
  radiusMin: 1, // Taille minimale possible d'une particule
  radiusMax: 2, // Taille maximale possible d'une particule
  fftSize: 1024,
};
