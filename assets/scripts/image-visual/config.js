const config = {
  density: 0.1, // nombre de particules par pixel
  xSpeed: 0.2, // speed at which the particles move
  ySpeed: 0.2,
  trailLength: 256, // number of frames the trail will exist for
  pAlpha: 0.3,
  maxColorSat: 255,
  radiusMin: 1, // Taille minimale possible d'une particule
  radiusMax: 1, // Taille maximale possible d'une particule
  randomLength: 128,
};
