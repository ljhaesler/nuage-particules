const config = {
  density: 0.1, // nombre de particules par pixel
  xSpeed: 0.5, // speed at which the particles move
  ySpeed: 0.5,
  trailLength: 512, // number of frames the trail will exist for
  pAlpha: 0.05,
  maxColorSat: 255,
  radiusMin: 4, // Taille minimale possible d'une particule
  radiusMax: 4, // Taille maximale possible d'une particule
  randomLength: 128,
};
