const config = {
  density: 0.0005, // nombre de particules par pixel
  xSpeed: 0.01, // speed at which the particles move along the x axis
  jitter: 0, // micro-agitation (Petits mouvements aléatoires indépendants)
  trailLength: 512, // number of frames the trail will exist for
  pAlpha: 1,
  maxColorSat: 255,
  maxPhase: 0, // the maximum phase offset for vector calculation along y axis
  radiusMin: 1, // Taille minimale possible d'une particule
  radiusMax: 1, // Taille maximale possible d'une particule
};
