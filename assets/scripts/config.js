const config = {
  density: 0.0001, // nombre de particules par pixel
  maxSpeed: 1, // vitesse maximale des particules
  flowStrength: 1, // influence du champ de flux (force du mouvement)
  jitter: 0, // micro-agitation (Petits mouvements aléatoires indépendants)
  friction: 0.5, // amortissement (ralentissement progressif)
  radiusMin: 1, // Taille minimale possible d'une particule
  radiusMax: 2, // Taille maximale possible d'une particule
  fadeAlpha: 0.05, // fond semi-transparent (traînées)
};
